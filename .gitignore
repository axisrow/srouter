# Локальная конфигурация (реальные адреса инфраструктуры)
srouter_config.py
srouter.local.json
srouter.local.json.tmp    # cycle-review #114 C3: save_state atomic-write temp (local_state.py:596)
*.srouter-backup-*        # _backup() timestamped — прежний (чужой) конфиг с секретами

# Opt-in локальный кэш hot_routes (домены+счётчики, privacy) — НИКОГДА не коммитить
srouter.hot_routes.json
srouter.hot_routes.json.tmp

# Секреты окружения
.env
.env.*
*.env
!.env.example

# Сгенерированные серверные артефакты и ключи
server/.generated/
server/generated/
server/rendered/
server/deploy-bundles/

# Local MCP config (may contain API keys)
.mcp.json

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/
build/
dist/

# Бэкапы и временное
*.bak
*.bak-*
dashboard.py.bak-*

# Диагностика с реальными сетевыми данными
diag-vpn.txt
diag-*.txt

# Служебные экспорты сессий
*local-command-caveat*.md

# Нерелевантные srouter файлы (остаются на диске)
CLAUDE.md
habr-claude-code-china.md
